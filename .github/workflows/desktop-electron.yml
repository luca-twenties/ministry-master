name: Desktop Electron Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'desktop/electron/**'
      - 'src/**'
      - 'react/**'
      - '.github/workflows/desktop-electron.yml'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js (from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.30.3 --activate || npm install -g pnpm@10.30.3
          pnpm --version

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo_sqlite, sqlite3, mbstring, intl, zip, curl, gd, fileinfo, gettext, mysqli, sodium
          coverage: none

      - name: Verify src/ChurchCRM exists
        shell: bash
        run: |
          ls -la
          ls -la src || true
          git ls-tree -d HEAD src || true
          git ls-tree -d HEAD src/ChurchCRM || true
          git ls-files 'src/ChurchCRM/*' | head -n 20 || true
          if [ ! -d "src/ChurchCRM" ]; then
            echo "Missing src/ChurchCRM in workspace"
            exit 1
          fi

      - name: Install PHP dependencies
        run: |
          composer --working-dir=src install --no-dev --no-interaction --prefer-dist --ignore-platform-reqs --no-scripts
          composer --working-dir=src dump-autoload --no-dev --ignore-platform-reqs --classmap-authoritative

      - name: Install Electron dependencies
        working-directory: desktop/electron
        env:
          PNPM_IGNORE_SCRIPTS: "false"
        run: pnpm install

      - name: Verify bundled PHP exists
        shell: bash
        run: |
          node -e "console.log(process.platform + '-' + process.arch)"
          PLATFORM_ARCH="$(node -e "process.stdout.write(process.platform + '-' + process.arch)")"
          BIN_NAME="php"
          if [ "$PLATFORM_ARCH" = "win32-x64" ] || [ "$PLATFORM_ARCH" = "win32-arm64" ]; then
            BIN_NAME="php.exe"
          fi
          PHP_PATH="desktop/electron/php/$PLATFORM_ARCH/$BIN_NAME"
          if [ ! -f "$PHP_PATH" ]; then
            echo "Missing PHP binary: $PHP_PATH"
            exit 1
          fi
          if [ "$RUNNER_OS" != "Windows" ]; then
            chmod +x "$PHP_PATH"
          fi

      - name: Build Electron app
        working-directory: desktop/electron
        env:
          PNPM_IGNORE_SCRIPTS: "false"
        run: pnpm run dist

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-electron-${{ matrix.os }}
          path: desktop/electron/dist/
